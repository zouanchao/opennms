<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <parent>
    <relativePath>../poms/compiled/</relativePath>
    <groupId>org.opennms.features.topology.build</groupId>
    <artifactId>compiled-bundle-settings</artifactId>
    <version>24.0.0-SNAPSHOT</version>
  </parent>

  <modelVersion>4.0.0</modelVersion>
  <groupId>org.opennms.features.topology</groupId>
  <artifactId>features</artifactId>

  <name>OpenNMS :: Features :: Topology :: Features</name>

  <packaging>pom</packaging>

  <modules>
    <module>api</module>
    <module>runtime-application</module>
    <module>runtime-asset</module>
    <module>runtime-base</module>
    <module>runtime-browsers</module>
    <module>runtime-bsm</module>
    <module>runtime-pathoutage</module>
    <module>runtime-graphml</module>
    <module>runtime-linkd</module>
    <module>runtime-vmware</module>
    <module>runtime-sfree</module>
  </modules>

  <build>
    <plugins>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-resources-plugin</artifactId>
      <executions>
        <execution>
          <id>copy-resources</id>
          <phase>generate-resources</phase>
          <goals><goal>copy-resources</goal></goals>
          <configuration>
            <outputDirectory>${project.build.outputDirectory}</outputDirectory>
            <resources>
              <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
              </resource>
            </resources>
          </configuration>
        </execution>
      </executions>
    </plugin>
    </plugins>
  </build>

</project>
